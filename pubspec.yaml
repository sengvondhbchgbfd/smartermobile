name: frontendmobile
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter


  # ----------------------------------------------


  image_picker: ^1.1.2
  permission_handler: ^11.3.1
  camera: ^0.12.0+1
  path_provider: ^2.1.2
  path: ^1.9.0
  intl: ^0.20.2
  mobile_scanner: ^5.1.1
  qr_flutter: ^4.1.0
  saver_gallery: ^4.1.1
  geolocator: ^13.0.2
  pdf: ^3.12.0
  share_plus: ^10.0.0
  flutter_map: ^8.3.0
  latlong2: ^0.9.1

  # ── new: required for chat ───────────────
  web_socket_channel: ^3.0.1
  file_picker: ^8.1.2
  cached_network_image: ^3.3.1

  




  # ----------------------------------------------
  equatable: ^2.0.5
  # ----------------------------------------------
  connectivity_plus: ^6.1.5
  injectable: ^2.5.0

  # ── UI ────────────────────────────────────────
  cupertino_icons: ^1.0.8

  # ── Navigation ────────────────────────────────
  go_router: ^14.0.0

  # ── State Management ──────────────────────────
  flutter_riverpod: ^2.5.1
  riverpod_annotation: ^2.3.5

  # ── HTTP & Networking ─────────────────────────
  dio: ^5.9.1
  pretty_dio_logger: ^1.4.0

  # ── Serialization ─────────────────────────────
  json_annotation: ^4.9.0
  freezed_annotation: ^3.0.0     

  # ── Local Storage ─────────────────────────────
  shared_preferences: ^2.3.3
  # flutter_secure_storage: ^9.2.2
  flutter_secure_storage: 8.1.0
  # flutter_secure_storage: ^9.0.0

  # ── Environment ───────────────────────────────
  flutter_dotenv: ^5.2.1

  # ── Functional Error Handling ─────────────────
  dartz: ^0.10.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  # ── Linting ───────────────────────────────────
  flutter_lints: ^5.0.0
  custom_lint: ^0.7.6
  riverpod_lint: ^2.3.0

  # ── Code Generation ───────────────────────────
  build_runner: ^2.4.13
  riverpod_generator: ^2.4.0
  json_serializable: ^6.9.0
  # analyzer: ^13.0.0                 # ← upgrade from ^2.5.7 to ^3.0.0





flutter:
  uses-material-design: true
  assets:
    - .env
    - assets/images/